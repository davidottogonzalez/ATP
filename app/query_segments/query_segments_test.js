'use strict';

describe('myApp.query_segments module', function() {

  beforeEach(module('myApp.query_segments'));

  describe('query_segments controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var query_segmentsCtrl = $controller('QuerySegmentsCtrl');
      expect(query_segmentsCtrl).toBeDefined();
    }));

  });
});