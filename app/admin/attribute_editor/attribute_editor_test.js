'use strict';

describe('myApp.attribute_editor module', function() {

  beforeEach(module('myApp.attribute_editor'));

  describe('attribute_editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var attribute_editorCtrl = $controller('AttributeEditorCtrl');
      expect(attribute_editorCtrl).toBeDefined();
    }));

  });
});