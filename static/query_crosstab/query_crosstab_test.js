'use strict';

describe('myApp.query_crosstab module', function() {

  beforeEach(module('myApp.query_crosstab'));

  describe('query_crosstab controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var query_crosstabCtrl = $controller('QueryCrossTabCtrl');
      expect(query_crosstabCtrl).toBeDefined();
    }));

  });
});