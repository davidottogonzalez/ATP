'use strict';

describe('myApp.attribute_builder module', function() {

  beforeEach(module('myApp.attribute_builder'));

  describe('attribute_builder controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var attribute_builderCtrl = $controller('AttributeBuilderCtrl');
      expect(attribute_builderCtrl).toBeDefined();
    }));

  });
});