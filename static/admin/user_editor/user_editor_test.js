'use strict';

describe('myApp.user_editor module', function() {

  beforeEach(module('myApp.user_editor'));

  describe('user_editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var user_editorCtrl = $controller('UserEditorCtrl');
      expect(user_editorCtrl).toBeDefined();
    }));

  });
});